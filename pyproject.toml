[project]
name = "rootcfo"
version = "1.0.0"
description = "Terminal-based financial fraud detection for SMEs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "textual",
    "mysql-connector-python",
    "groq",
    "python-dotenv",
    "pandas",
    "bcrypt",
    "textual-dev>=1.8.0",
    "dotenv>=0.9.9",
]
